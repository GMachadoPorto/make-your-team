import { StyledDiv } from "./style";

export const SearchPokeName = () => {
  return (
    <StyledDiv>
      <input placeholder="Pesquise o nome..." />
      <button>Lupa</button>
    </StyledDiv>
  );
};
